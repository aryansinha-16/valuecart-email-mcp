{
  "name": "valuecart-email-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that sends emails via Gmail SMTP — for use with Cowork scheduled tasks",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "express": "^4.19.2",
    "nodemailer": "^6.9.13",
    "zod": "^3.23.8"
  }
}
