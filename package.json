{
  "name": "valuecart-email-mcp-server",
  "version": "1.3.0",
  "description": "MCP server for sending emails via SendGrid — for use with Cowork scheduled tasks",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "@sendgrid/mail": "^8.1.3",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  }
}
